{
  "type": "object",
  "additionalProperties": false,
  "required": ["walletAddress"],
  "properties": {
    "info": {
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "ui": {
            "label": "Name",
            "hint": "The name of your project",
            "placeholder": "My Project"
          }
        },
        "sex": {
          "type": "string",
          "ui": {
            "label": "Name",
            "hint": "The name of your project",
            "placeholder": "My Project"
          }
        }
      }
    },
    "description": {
      "type": "string",
      "format": "address"
    },
    "externalUrl": {
      "type": "string"
    },
    "walletAddress": {
      "type": "string",
      "format": "address",
      "minLength": 1
    }
  }
}
